# Phase 4.4 归档报告 — SQLite 主数据源 + 幂等同步验证

**日期**: 2026-05-19  
**状态**: ✅ 条件通过，可归档  
**审查**: Codex 最终审查 6/6 PASS（代码层面）

---

## 1. 修改范围

### 新建文件（11 个）

| # | 文件 | 用途 |
|---|------|------|
| F1 | `local_api/routers/sync.py` | 同步状态 CRUD 路由 |
| F2 | `local_api/routers/sync_logs.py` | 同步日志查询路由 |
| F3 | `local_api/sync_models.py` | SyncState/SyncLog Pydantic 模型 |
| F4 | `local_api/services/__init__.py` | Services 包初始化 |
| F5 | `local_api/services/sync_state_service.py` | sync_state 业务逻辑 |
| F6 | `local_api/services/sync_log_service.py` | sync_log 写入与查询 |
| F7 | `local_api/sync_client/__init__.py` | sync_client 包初始化 |
| F8 | `local_api/sync_client/payload.py` | payload_hash SHA256 计算 |
| F9 | `local_api/tests/test_sync_api.py` | 同步 API 测试（33 用例） |
| F10 | `local_api/tests/test_sync_services.py` | 服务层测试（31 用例） |
| F11 | `local_api/scripts/verify_4_4.sh` | bash 端到端验证 |

### 修改文件（5 个）

| # | 文件 | 修改内容 |
|---|------|---------|
| M1 | `local_api/database.py` | SCHEMA_SQL 追加 sync_state/sync_logs 表 + tasks 扩展列 |
| M2 | `local_api/config.py` | 新增同步常量（状态枚举/target 枚举/sync_result 枚举） |
| M3 | `local_api/main.py` | 注册 sync_router/sync_logs_router，version → 4.4.0 |
| M4 | `local_api/routers/__init__.py` | 导出 sync_router/sync_logs_router |
| M5 | `local_api/tests/__init__.py` | 测试包导入适配 |

---

## 2. 5 个 Codex 修复点

| # | 修复 | 文件:行号 |
|---|------|----------|
| 1 | `main.py` version 4.3.0 → 4.4.0 | `main.py:53,74` |
| 2 | `create_sync_state()` 对不存在 task_id → ValueError | `services/sync_state_service.py:69` |
| 3 | `create_sync_log()` 对不存在 sync_id → ValueError | `services/sync_log_service.py:52` |
| 4 | `get_sync_state_by_key()` 非法 sync_target → ValueError | `services/sync_state_service.py:110` |
| 5 | `error_message` 强制 REDACTED | `services/sync_log_service.py:15,47,65` |

---

## 3. 测试结果

| 测试套件 | 结果 |
|----------|------|
| sync_services (31) | ✅ 31/31 PASSED |
| sync_api (33) | ✅ 33/33 PASSED |
| 全量回归 (120) | ⚠️ 119/120 PASSED |

### 唯一失败：`test_pipe_rejected`

| 项目 | 详情 |
|------|------|
| 测试 | `TestShellInjection::test_pipe_rejected` |
| 文件 | `tests/test_api.py:494` |
| 失败原因 | `SHELL_PATTERNS` 仅有 `\|\|`（双管道），缺少单管道 `\|` 规则 |
| 归属 | **4.3 预存问题**——`validators.py` 时间戳 5/18 23:13（早于 4.4），本轮未修改 |
| 影响 | 单管道 `\|` 可绕过 shell injection 检测 |
| 建议 | Phase 4.5 在 `SHELL_PATTERNS` 中补充 `re.compile(r"\|")` |

---

## 4. 清理前后 git 状态

### 清理前

| 运行产物 | 数量 |
|----------|------|
| `__pycache__/` | 6 个目录 |
| `test_data/*.db` | 9 个文件（含 xdist worker DBs） |
| `logs/access.log` | 1 个文件 |
| `.pytest_cache/` | 1 个目录（xdist 锁定，无法删除） |

### 清理后

| 项目 | 状态 |
|------|------|
| `__pycache__/` | ✅ 0 |
| `test_data/*.db` | ✅ 0 |
| `logs/access.log` | ✅ CLEAN |
| `.pytest_cache/` | ⚠️ xdist 锁（预存，非 4.4 产物） |

---

## 5. 设计规范确认

| 检查项 | 状态 |
|--------|------|
| sync_target 仅 apple_calendar/apple_reminder | ✅ CHECK 约束 + 枚举校验 |
| sync_key `"{task_id}:{sync_target}"` | ✅ `services/sync_state_service.py` |
| sync_status 10 状态完整 | ✅ `config.py:67` + DB CHECK |
| 禁止状态 0 | ✅ canceled/created/syncing 等 0 命中 |
| Terminal-True/Auto 分类 | ✅ 设计已锁定 |
| payload_hash SHA256 | ✅ `sync_client/payload.py` |
| REDACTED 脱敏 | ✅ error_message 强制替换 |
| 无 Apple/Weather/WeChat 调用 | ✅ Codex 确认 |

---

## 6. 残余风险

| 风险 | 等级 | 说明 |
|------|------|------|
| `test_pipe_rejected` 未修复 | 低 | 4.3 预存，单管道绕过 |
| `.pytest_cache` 未清理 | 低 | xdist 权限锁，非 4.4 产物 |
| `local_api/` 未被 git 跟踪 | 低 | 父仓库 `.gitignore` 仅忽略 db/log，源码应跟踪 |
| verify_4_4.sh 待首次运行 | 低 | 已实现，需启动 uvicorn 后验证 |

---

## 7. Phase 4.5 建议

1. **修复 SHELL_PATTERNS**：在 `validators.py` 中补充单管道 `\|` regex
2. **首次运行 verify_4_4.sh**：启动服务后执行 bash 验证
3. **git 跟踪 local_api/ 源码**：将 `.py`/`.sh` 文件加入 git
4. **sync engine 实现**：当前为 CRUD + 校验框架，核心同步引擎（状态机驱动、Apple 投影模拟）待实现

---

## 8. 最终结论

**Phase 4.4 条件通过，可归档。**

| 维度 | 结论 |
|------|------|
| 代码层面 | ✅ 6/6 PASS（Codex 审查） |
| 测试 | ✅ 119/120（1 个 4.3 预存问题） |
| 白名单合规 | ✅ 16 文件全到位，无越权修改 |
| 运行时残留 | ✅ 已清理（`.pytest_cache` 除外） |
| 设计规范 | ✅ 10 状态/sync_target/sync_key/REDACTED 全部落地 |
